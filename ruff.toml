# Lint config for the v2 Python (docker/offload, tests). legacy/v1 has its own pyproject.
target-version = "py312"
line-length = 100

[lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["E501"]  # formatter owns line length
